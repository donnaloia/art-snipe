[project]
name = "artpipe"
version = "0.1.0"
description = "AI-assisted game asset pipeline scaffold for Godot"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
artpipe = "artpipe.cli:main"
